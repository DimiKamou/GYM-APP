# Από πιλότο σε προϊόν — τι είναι έτοιμο και τι όχι

Ο στόχος άλλαξε: η εφαρμογή θα δοθεί και σε **άλλα γυμναστήρια**, με χρέωση.
Αυτό το αρχείο είναι η παράδοση για όποιον συνεχίσει — ό,τι έμαθε ο πιλότος και
δεν φαίνεται από τον κώδικα.

## Τι αντέχει ήδη πολλά γυμναστήρια

Το σκληρό κομμάτι είναι γραμμένο και δοκιμασμένο:

- **Η απομόνωση ανά γυμναστήριο είναι πραγματική.** 44 policies, κάθε πίνακας
  κλειδωμένος σε `app.my_gym()`, και το `supabase/tests/01_rls_test.sql` το
  ελέγχει με **δύο** γυμναστήρια — όχι υποθετικά: προσπαθεί να διαβάσει τα ξένα
  δεδομένα και επιβεβαιώνει ότι δεν τα βλέπει.
- **Εγγραφή και προσκλήσεις υπάρχουν ήδη στη βάση**: `bootstrap_gym()` φτιάχνει
  γυμναστήριο με τον πρώτο ιδιοκτήτη, `create_invite()` / `redeem_invite()`
  βάζουν γυμναστές. Καμία οθόνη δεν τα χρησιμοποιεί ακόμα πλήρως.
- Κάθε `@st.cache_data` κλειδώνεται σε `gym_id`, γιατί η cache είναι καθολική
  στη διεργασία και θα ήταν η διαρροή που το RLS απέτρεψε.

## Τι πρέπει να ξηλωθεί πριν μπει δεύτερο γυμναστήριο

**1. Το migration 006 πρέπει να αναιρεθεί ή να ξαναγραφτεί.**

Έδωσε ΟΛΟΝ τον κοινό κατάλογο ασκήσεων στο PowerHouseGym, ώστε να μπορεί να τον
επεξεργάζεται. Για ένα γυμναστήριο ήταν σωστό. Για δέκα είναι λάθος: το δεύτερο
γυμναστήριο ξεκινάει με **άδειο κατάλογο**. Το ίδιο το 006 αρνείται να τρέξει με
πάνω από ένα γυμναστήριο ακριβώς γι' αυτό.

Η σωστή λύση για προϊόν: ο κοινός κατάλογος μένει κοινός και read-only, και κάθε
γυμναστήριο αποκτά ΔΙΚΕΣ του γραμμές μόνο όταν αλλάξει κάτι (copy-on-write), ή
ένας πίνακας «κρυμμένες/μετονομασμένες για αυτό το γυμναστήριο».

**2. Το `service_role` κλειδί.**

Είναι στα Streamlit secrets και είχε εκτεθεί σε συνομιλία. Ο ιδιοκτήτης το
αποδέχτηκε όσο ήταν ένα ιδιωτικό γυμναστήριο με δικά του δεδομένα — εύλογο τότε.
Με δεδομένα ΑΛΛΩΝ γυμναστηρίων πάνω του, δεν είναι: το κλειδί παρακάμπτει κάθε
policy για κάθε ενοικιαστή. Πριν μπει τρίτος, αλλάζει.

**3. Η φιλοξενία.**

Το Streamlit Community Cloud είναι δωρεάν φιλοξενία κοινότητας — κοιμάται, δεν
δίνει εγγυήσεις, δεν παίρνει domain. Δούλεψε άψογα για πιλότο. Δεν είναι πλατφόρμα
να χρεώνεις από πάνω της. Το PWA στο `src/` είναι ο δρόμος: static αρχεία, τίποτα
να πέσει, δωρεάν domain.

**4. Χρέωση, όροι χρήσης, GDPR.**

Δεν υπάρχει τίποτα από αυτά. Δεδομένα υγείας/σωματομετρικά τρίτων σε ελληνικό
γυμναστήριο σημαίνει υποχρεώσεις που δεν είναι κώδικας.

## Τι λείπει από το PWA (`src/`)

Χτίζει καθαρά και περνάει 282 τεστ, αλλά:

- **Μπαίνει με κωδικό μιας χρήσης σε email.** Οι λογαριασμοί του πιλότου είναι
  ψεύτικες διευθύνσεις (`dimitris@powerhouse.gr`) που δεν λαμβάνουν μήνυμα, άρα
  κανείς δεν μπορεί να μπει. Χρειάζεται χρήστη+κωδικό όπως το Streamlit, ή
  αληθινά email.
- **Δεν δουλεύει offline** (ο service worker είχε αναβληθεί).
- **Δεν το έχει χρησιμοποιήσει ποτέ άνθρωπος.** Όλα τα σφάλματα του πιλότου
  βγήκαν από πραγματικά κινητά, όχι από τεστ.

## Πώς δοκιμάζεται

- `cd streamlit && python3 tests/run.py` — 114 έλεγχοι, πατάνε αληθινά κουμπιά.
- `cd streamlit && streamlit run tests/demo_app.py` — όλη η εφαρμογή στον browser
  με ψεύτικη βάση, χωρίς Supabase.
- `bash supabase/tests/run.sh` — το RLS σε πραγματική Postgres.
- `npm test && npm run build` — το PWA.

Το `streamlit/tests/fake_supabase.py` επιβάλλει πλέον το unique index στα
ονόματα ασκήσεων. Μην το χαλαρώσεις: η απουσία του άφησε να περάσει ένα ολόκληρο
σχέδιο που η Postgres απορρίπτει.
